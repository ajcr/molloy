from setuptools import setup

setup(name='molloy',
      version='0.1',
      description='Compute counts of combinations',
      url='https://github.com/ajcr/molloy',
      author='Alex Riley',
      license='MIT',
      packages=['molloy'],
      install_requires=['numpy'],
      zip_safe=False)
